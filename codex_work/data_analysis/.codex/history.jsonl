{"session_id":"019c21cb-b401-7561-b9e7-5c7468469e00","ts":1770094069,"text":"\n## User Input\n\n```text\nThis repository is governed by the following constitution. All code, file structure, tooling, and documentation MUST comply. PURPOSE The purpose of this repository is to clean, process, and analyze large-scale Twitter data provided in CSV format for Super Bowl–related analysis. The analysis MUST support: Brand and ad volume metrics Sentiment analysis (by ad, by brand, over time) Time-based analysis (quarters, minute-by-minute, before/after game) ROI-related metrics (cost, followers, retweets, engagement proxies) Variable relationship analysis Event-based analysis (game events, ad timing) Text mining and network analysis (hashtags, keywords, co-occurrence) The system MUST be repeatable for every Super Bowl year and MUST allow identical analyses to be re-run on new yearly datasets with no code changes. DATA ORGANIZATION (MANDATORY) All data MUST be organized by Super Bowl year. The following directory layout is REQUIRED: data/ raw/ <year>/ *.csv processed/ <year>/ enriched/ <year>/ analytics/ <year>/ Rules: Raw data is read-only once ingested. No script may modify files in data/raw. Each year’s data MUST be isolated to its own directory. Scripts MUST accept a year or data directory as an explicit input. Cross-year analysis is only allowed via explicit aggregation steps. PIPELINE ARCHITECTURE (MANDATORY) The pipeline MUST be modular and stage-based. Each stage MUST be independently runnable: ingest clean process analyze visualize (optional) export Each stage MUST: Take explicit input paths Produce explicit output artifacts Be callable independently from the CLI Be callable as part of a full end-to-end pipeline run There MUST be an option to run: A single stage for a single year The full pipeline for a single year The full pipeline for all available years CLI REQUIREMENTS A CLI interface MUST exist. The CLI MUST allow: Selecting a year OR a data directory Selecting a single pipeline stage OR the full pipeline Running in deterministic mode (same input → same output) Example concepts (not exact syntax): run --year 2024 --stage clean run --year 2024 --all run --data-dir data/raw/2023 --all ANALYTICS REQUIREMENTS The analytics layer MUST support: Metric extraction by brand and ad Sentiment aggregation over time Time-bucketed analysis (quarters, minutes, before/after) Relationship analysis between engagement, sentiment, and volume Event-aligned analysis (game events, ad timestamps) Text mining (hashtags, keywords) Network or co-occurrence analysis when applicable Analytics outputs MUST be stored per year and MUST NOT overwrite other years. REPRODUCIBILITY & SCALE All steps MUST be deterministic. Intermediate artifacts MUST be persisted to disk. Large datasets MUST be processed in a streaming or chunked manner where applicable. No stage may assume data fits entirely in memory. DOCUMENTATION (STRICTLY ENFORCED) A README.md MUST exist at the repository root. If it does not exist, it MUST be created. Every new feature, pipeline stage, CLI command, or analytical capability MUST: Update README.md Explain what the feature does Explain how to run it Explain required inputs and produced outputs Failure to update documentation is a violation of this constitution. EXTENSIBILITY The architecture MUST allow: Adding new Super Bowl years with no code changes Adding new analysis modules without breaking existing ones Future extension to additional social platforms without restructuring core directories ENFORCEMENT If there is ambiguity: Prefer modularity over convenience Prefer explicit configuration over implicit behavior Prefer reproducibility over speed Prefer clarity over cleverness Any implementation that violates these rules MUST be refactored to comply.\n```\n\nYou **MUST** consider the user input before proceeding (if not empty).\n\n## Outline\n\nYou are updating the project constitution at `.specify/memory/constitution.md`. This file is a TEMPLATE containing placeholder tokens in square brackets (e.g. `[PROJECT_NAME]`, `[PRINCIPLE_1_NAME]`). Your job is to (a) collect/derive concrete values, (b) fill the template precisely, and (c) propagate any amendments across dependent artifacts.\n\nFollow this execution flow:\n\n1. Load the existing constitution template at `.specify/memory/constitution.md`.\n   - Identify every placeholder token of the form `[ALL_CAPS_IDENTIFIER]`.\n   **IMPORTANT**: The user might require less or more principles than the ones used in the template. If a number is specified, respect that - follow the general template. You will update the doc accordingly.\n\n2. Collect/derive values for placeholders:\n   - If user input (conversation) supplies a value, use it.\n   - Otherwise infer from existing repo context (README, docs, prior constitution versions if embedded).\n   - For governance dates: `RATIFICATION_DATE` is the original adoption date (if unknown ask or mark TODO), `LAST_AMENDED_DATE` is today if changes are made, otherwise keep previous.\n   - `CONSTITUTION_VERSION` must increment according to semantic versioning rules:\n     - MAJOR: Backward incompatible governance/principle removals or redefinitions.\n     - MINOR: New principle/section added or materially expanded guidance.\n     - PATCH: Clarifications, wording, typo fixes, non-semantic refinements.\n   - If version bump type ambiguous, propose reasoning before finalizing.\n\n3. Draft the updated constitution content:\n   - Replace every placeholder with concrete text (no bracketed tokens left except intentionally retained template slots that the project has chosen not to define yet—explicitly justify any left).\n   - Preserve heading hierarchy and comments can be removed once replaced unless they still add clarifying guidance.\n   - Ensure each Principle section: succinct name line, paragraph (or bullet list) capturing non‑negotiable rules, explicit rationale if not obvious.\n   - Ensure Governance section lists amendment procedure, versioning policy, and compliance review expectations.\n\n4. Consistency propagation checklist (convert prior checklist into active validations):\n   - Read `.specify/templates/plan-template.md` and ensure any \"Constitution Check\" or rules align with updated principles.\n   - Read `.specify/templates/spec-template.md` for scope/requirements alignment—update if constitution adds/removes mandatory sections or constraints.\n   - Read `.specify/templates/tasks-template.md` and ensure task categorization reflects new or removed principle-driven task types (e.g., observability, versioning, testing discipline).\n   - Read each command file in `.specify/templates/commands/*.md` (including this one) to verify no outdated references (agent-specific names like CLAUDE only) remain when generic guidance is required.\n   - Read any runtime guidance docs (e.g., `README.md`, `docs/quickstart.md`, or agent-specific guidance files if present). Update references to principles changed.\n\n5. Produce a Sync Impact Report (prepend as an HTML comment at top of the constitution file after update):\n   - Version change: old → new\n   - List of modified principles (old title → new title if renamed)\n   - Added sections\n   - Removed sections\n   - Templates requiring updates (✅ updated / ⚠ pending) with file paths\n   - Follow-up TODOs if any placeholders intentionally deferred.\n\n6. Validation before final output:\n   - No remaining unexplained bracket tokens.\n   - Version line matches report.\n   - Dates ISO format YYYY-MM-DD.\n   - Principles are declarative, testable, and free of vague language (\"should\" → replace with MUST/SHOULD rationale where appropriate).\n\n7. Write the completed constitution back to `.specify/memory/constitution.md` (overwrite).\n\n8. Output a final summary to the user with:\n   - New version and bump rationale.\n   - Any files flagged for manual follow-up.\n   - Suggested commit message (e.g., `docs: amend constitution to vX.Y.Z (principle additions + governance update)`).\n\nFormatting & Style Requirements:\n\n- Use Markdown headings exactly as in the template (do not demote/promote levels).\n- Wrap long rationale lines to keep readability (<100 chars ideally) but do not hard enforce with awkward breaks.\n- Keep a single blank line between sections.\n- Avoid trailing whitespace.\n\nIf the user supplies partial updates (e.g., only one principle revision), still perform validation and version decision steps.\n\nIf critical info missing (e.g., ratification date truly unknown), insert `TODO(<FIELD_NAME>): explanation` and include in the Sync Impact Report under deferred items.\n\nDo not create a new template; always operate on the existing `.specify/memory/constitution.md` file.\n"}
{"session_id":"019c21cb-b401-7561-b9e7-5c7468469e00","ts":1770095146,"text":"\n## User Input\n\n```text\nThis plan defines the recommended implementation approach for the Twitter Super Bowl Analytics repository governed by the constitution. The goal is to build a modular, repeatable, year-isolated data pipeline with a strong CLI interface and strict documentation discipline. PHASE 0 — REPOSITORY BOOTSTRAP Initialize repository structure Create top-level directories: data/ src/ src/cli/ src/pipeline/ src/analytics/ src/utils/ specs/ Ensure data/ contains raw/, processed/, enriched/, analytics/ subfolders. This phase MUST complete before any pipeline logic is written. PHASE 1 — CONFIGURATION & CONTRACTS Define a shared configuration model Year-based configuration (year, input paths, output paths). Pipeline-stage configuration (enabled stages, parameters). Ensure configuration can be passed via CLI arguments. Define pipeline stage contracts Each stage MUST: Accept explicit input paths Accept explicit output paths Expose a single callable entry point Return structured metadata (row counts, artifacts written) Define a canonical schema representation Expected columns for raw Twitter CSVs Optional columns Validation rules Schema checks occur during ingestion No analytics logic should be written before contracts are finalized. PHASE 2 — CLI IMPLEMENTATION Implement a single CLI entry point Support: --year <YYYY> --data-dir <path> --stage <stage-name> --all (run full pipeline) Enforce mutual exclusivity where required. CLI responsibilities Parse arguments Resolve year → directory paths Validate inputs exist Dispatch execution to pipeline orchestrator CLI must be deterministic and stateless No hidden defaults No implicit file discovery outside declared directories Update README.md with every supported CLI command. PHASE 3 — PIPELINE ORCHESTRATION Implement a pipeline orchestrator Accepts: year or data directory list of stages to run Executes stages sequentially Passes artifacts between stages via disk, not memory Orchestrator responsibilities Enforce stage ordering Skip stages only when explicitly requested Validate required upstream artifacts exist Artifact tracking Each stage writes outputs to its own year-scoped directory Metadata files track: input files output files execution timestamp record counts PHASE 4 — DATA INGESTION & CLEANING Ingestion stage Load raw CSVs from data/raw/<year>/ Validate schema Reject or log malformed files Write validated outputs to processed/<year>/ Cleaning stage De-duplicate tweets Normalize timestamps Handle nulls and malformed records Preserve original content alongside cleaned fields Raw data MUST never be modified. Update README.md with ingestion and cleaning behavior. PHASE 5 — PROCESSING & FEATURE EXTRACTION Processing stage Text normalization Hashtag extraction Keyword extraction Brand and ad tagging (rule-based or lookup-driven) Output Write enriched datasets to enriched/<year>/ Ensure outputs are chunked or streamed for scale All processing MUST be repeatable and deterministic. PHASE 6 — ANALYTICS IMPLEMENTATION Analytics modules should be isolated by concern: Volume metrics Sentiment analysis Time-based aggregation ROI proxy metrics Relationship analysis Event-aligned analysis Text/network analysis Each analytics module MUST: Accept enriched data as input Output year-scoped analytics artifacts Be callable independently No analytics module may assume exclusive ownership of data. Document each analytic capability in README.md. PHASE 7 — FULL PIPELINE EXECUTION Implement full-pipeline execution mode Run all stages in correct order Accept year or data directory Fail fast on schema or contract violations Ensure running the full pipeline produces: Processed data Enriched data Analytics outputs Execution metadata PHASE 8 — EXTENSIBILITY & HARDENING Ensure adding a new year requires: Dropping CSVs into data/raw/<year>/ Running the same CLI command Ensure adding a new analytics module requires: No changes to existing modules Minimal orchestrator updates Add guardrails: Clear error messages Explicit validation failures No silent skipping of steps PHASE 9 — DOCUMENTATION ENFORCEMENT README.md MUST be updated whenever: A new pipeline stage is added A new CLI option is introduced A new analytics capability is implemented README.md MUST always describe: What exists Why it exists How to run it Documentation updates are not optional.\n```\n\nYou **MUST** consider the user input before proceeding (if not empty).\n\n## Outline\n\n1. **Setup**: Run `.specify/scripts/bash/setup-plan.sh --json` from repo root and parse JSON for FEATURE_SPEC, IMPL_PLAN, SPECS_DIR, BRANCH. For single quotes in args like \"I'm Groot\", use escape syntax: e.g 'I'\\''m Groot' (or double-quote if possible: \"I'm Groot\").\n\n2. **Load context**: Read FEATURE_SPEC and `.specify/memory/constitution.md`. Load IMPL_PLAN template (already copied).\n\n3. **Execute plan workflow**: Follow the structure in IMPL_PLAN template to:\n   - Fill Technical Context (mark unknowns as \"NEEDS CLARIFICATION\")\n   - Fill Constitution Check section from constitution\n   - Evaluate gates (ERROR if violations unjustified)\n   - Phase 0: Generate research.md (resolve all NEEDS CLARIFICATION)\n   - Phase 1: Generate data-model.md, contracts/, quickstart.md\n   - Phase 1: Update agent context by running the agent script\n   - Re-evaluate Constitution Check post-design\n\n4. **Stop and report**: Command ends after Phase 2 planning. Report branch, IMPL_PLAN path, and generated artifacts.\n\n## Phases\n\n### Phase 0: Outline & Research\n\n1. **Extract unknowns from Technical Context** above:\n   - For each NEEDS CLARIFICATION → research task\n   - For each dependency → best practices task\n   - For each integration → patterns task\n\n2. **Generate and dispatch research agents**:\n\n   ```text\n   For each unknown in Technical Context:\n     Task: \"Research {unknown} for {feature context}\"\n   For each technology choice:\n     Task: \"Find best practices for {tech} in {domain}\"\n   ```\n\n3. **Consolidate findings** in `research.md` using format:\n   - Decision: [what was chosen]\n   - Rationale: [why chosen]\n   - Alternatives considered: [what else evaluated]\n\n**Output**: research.md with all NEEDS CLARIFICATION resolved\n\n### Phase 1: Design & Contracts\n\n**Prerequisites:** `research.md` complete\n\n1. **Extract entities from feature spec** → `data-model.md`:\n   - Entity name, fields, relationships\n   - Validation rules from requirements\n   - State transitions if applicable\n\n2. **Generate API contracts** from functional requirements:\n   - For each user action → endpoint\n   - Use standard REST/GraphQL patterns\n   - Output OpenAPI/GraphQL schema to `/contracts/`\n\n3. **Agent context update**:\n   - Run `.specify/scripts/bash/update-agent-context.sh codex`\n   - These scripts detect which AI agent is in use\n   - Update the appropriate agent-specific context file\n   - Add only new technology from current plan\n   - Preserve manual additions between markers\n\n**Output**: data-model.md, /contracts/*, quickstart.md, agent-specific file\n\n## Key rules\n\n- Use absolute paths\n- ERROR on gate failures or unresolved clarifications\n"}
{"session_id":"019c21cb-b401-7561-b9e7-5c7468469e00","ts":1770095544,"text":"This is great, but before you do anything please move the files back into data_analysis. The directory is meant to contain all files needed for data analysis, and the parent folder will contain other directories that will work for other purposes."}
{"session_id":"019c21cb-b401-7561-b9e7-5c7468469e00","ts":1770095653,"text":"Please add in the constitution that all work regarding this data analysis directory must remain within the data analysis directory, creating new subdirectories if needed."}
